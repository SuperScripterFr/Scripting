-- ⚡ SINGLE TARGET FOCUSED AURA

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer

-- CONFIG
local RANGE = 30
local TOUCH_BURST = 2
local AURA_RATE = 1/40 -- slightly faster now that it's lighter

local lastAura = 0

-- HELPERS
local function getHRP(plr)
	local c = plr.Character
	return c and c:FindFirstChild("HumanoidRootPart")
end

-- ✅ FIND BEST TARGET (nearest)
local function getBestTarget()
	local myRoot = getHRP(LocalPlayer)
	if not myRoot then return end

	local best, dist = nil, 25

	for _,p in ipairs(Players:GetPlayers()) do
		if p ~= LocalPlayer then
			local r = getHRP(p)
			if r then
				local d = (r.Position - myRoot.Position).Magnitude
				if d < dist then
					dist = d
					best = r
				end
			end
		end
	end

	return best
end

-- ⚡ ONE CLEAN HIT ONLY
firetouchinterest(toolPart, targetHRP, 0)
firetouchinterest(toolPart, targetHRP, 1)

-- ⚡ CORE LOOP
RunService.PreSimulation:Connect(function()

	local char = LocalPlayer.Character
	if not char then return end

	local now = os.clock()
	if now - lastAura < AURA_RATE then return end
	lastAura = now

	local targetHRP = getBestTarget()
	if not targetHRP then return end

	-- ⚡ stabilization (VERY IMPORTANT)
	RunService.PostSimulation:Wait()

	-- ✅ ALL TOOLS → ONE TARGET HRP
	for _, tool in ipairs(char:GetChildren()) do
		if tool:IsA("Tool") then

			if tool.Parent ~= char then
				tool.Parent = char
			end

			local touch = tool:FindFirstChildWhichIsA("TouchTransmitter", true)
			touch = touch and touch.Parent

			if touch then
				tap(touch, targetHRP)
			end
		end
	end

end)

print("⚡ Focused Aura Loaded")
